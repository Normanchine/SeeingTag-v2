# 定位程序高速化改进方案

## 目标

让调试定位程序更接近比赛现场的 60fps / 低延迟链路，减少无效图像处理和显示开销，使 UDP 定位发送频率尽量接近摄像头真实输入频率。

核心目标不是单纯追求窗口显示 60fps，而是：

- 定位结果更新更快。
- UDP 发送延迟更低。
- 处理链路不积压旧帧。
- 调试窗口保留必要信息，但不能拖慢定位主链路。
- H 矩阵在相机和固定 Tag 不变时只初始化一次。

## 当前主要问题

当前比赛循环基本是串行流程：

```text
cap.read
-> 灰度转换
-> Tag 检测
-> H / 鸟瞰转换
-> 坐标滤波
-> UDP 发送
-> HUD 绘制
-> 鸟瞰窗口生成
-> 赛道图窗口生成
-> imshow / waitKey
```

这意味着任何一步变慢，都会直接降低定位发送频率。

当前配置里已经有：

```json
"homography_update_mode": "once",
"bird_eye_only": true
```

这说明固定摄像头场景下，H 矩阵本来就不需要每帧计算。但在 `bird_eye_only` 模式中，程序仍可能每帧生成高分辨率鸟瞰图：

```python
tracker.warp_to_bird_eye(frame, 1600, 1280)
```

这一步开销很大，容易成为 60fps 的主要瓶颈。

## 推荐总体架构

```text
启动阶段：
  检测固定 Tag
  计算一次 H
  进入比赛循环

采集线程：
  持续 cap.read
  只保存最新帧
  旧帧直接覆盖

定位处理主循环：
  取最新帧
  原图检测车 Tag
  使用缓存 H 映射坐标
  失败时才启用鸟瞰兜底
  定位成功后立即 UDP 发送

显示刷新：
  原图 HUD 可选显示
  鸟瞰小窗口低频显示
  赛道图低频显示
  不参与定位发送节奏
```

## 改进项 1：H 矩阵固定化

### 建议

比赛模式默认使用：

```json
"homography_update_mode": "once"
```

启动时检测全部固定 Tag，计算一次 H。进入比赛后不再自动更新 H。

只有以下情况才重新计算 H：

- 手动按 `R` 重新校准。
- 相机移动。
- 固定 Tag 位置变化。
- 赛道重新摆放。

### 理由

你们的定位相机和固定 Tag 如果不变，H 矩阵就是固定的。每帧重新检测固定 Tag 或更新 H，只会增加开销和不稳定性。

## 改进项 2：摄像头采集线程

### 建议

新增一个 `LatestFrameCamera` 类：

```text
启动一个后台线程持续读取摄像头
用锁保存最新 frame
主线程每次只取最新 frame
不建立帧队列
```

### 理由

定位程序最怕“处理不过来还排队”。如果摄像头 60fps，处理只有 35fps，排队会让车拿到越来越旧的位置。

正确策略是：

```text
旧帧没有价值，直接丢。
永远处理最新帧。
```

这样即使处理频率不满 60fps，延迟也不会持续累积。

## 改进项 3：定位和显示解耦

### 建议

把比赛主循环分成两条节奏：

```text
定位节奏：尽可能快，每帧处理，每次成功立即 UDP 发送。
显示节奏：固定 10~15fps，用于观察状态。
```

新增配置：

```json
"display_enabled": true,
"display_fps": 12,
"show_original_view": true,
"show_bird_eye_view": false,
"show_track_map": true
```

### 理由

`cv2.imshow`、HUD 绘制、赛道图绘制、鸟瞰图绘制都不应该决定 UDP 发送频率。

比赛时真正重要的是定位发送，不是窗口刷新。

## 改进项 4：鸟瞰图按需生成

### 建议

默认流程改成：

```text
优先原图识别车 Tag
成功：直接用缓存 H 计算坐标并发送
失败：才生成鸟瞰图兜底
```

显示用鸟瞰图和兜底用鸟瞰图分开：

```text
兜底鸟瞰：可以高分辨率，例如 1600x1280，但只在原图失败时生成。
显示鸟瞰：低分辨率，例如 800x640 或更小，且按 display_fps 刷新。
```

### 理由

每帧生成高分辨率鸟瞰图是重操作。鸟瞰图有价值，但不应该每帧无条件生成。

## 改进项 5：性能统计

### 建议

新增轻量性能统计，每秒输出：

```text
camera_fps
process_fps
send_fps
read_ms
detect_ms
warp_ms
display_ms
frame_avg_ms
frame_p95_ms
```

### 判断标准

如果目标是 60fps：

```text
单帧 P95 处理时延 <= 16.7ms
```

如果目标是 30fps：

```text
单帧 P95 处理时延 <= 33.3ms
```

官方提到 `p95 帧处理时延约 17ms`，对应的就是 60fps 附近的处理预算。

## 改进项 6：项目级拆分

当前所有逻辑集中在 `main.py`，后续维护会越来越困难。建议逐步拆成：

```text
main.py              参数解析、模式入口
competition.py       比赛主循环
camera_worker.py     摄像头采集线程
tracker.py           TagTracker、H、坐标转换
display.py           HUD、鸟瞰窗口、赛道窗口
perf_stats.py        性能统计
udp_sender.py        UDP 发送
blind_drive.py       盲开预测和状态管理
```

第一阶段可以先不拆文件，只在 `main.py` 内做高速模式，跑通后再模块化。

## 建议实施顺序

### 第一阶段：低风险提速

1. 默认保持 `homography_update_mode = once`。
2. 增加 `display_fps`，把显示限制到 10~15fps。
3. 取消每帧高分辨率鸟瞰显示。
4. 增加性能统计。

这一阶段改动小，最容易验证收益。

### 第二阶段：采集线程

1. 增加最新帧采集线程。
2. 主循环只取最新帧。
3. 不使用帧队列，避免延迟积压。
4. 统计 camera_fps / process_fps / send_fps。

这一阶段可以明显降低端到端延迟。

### 第三阶段：定位链路重排

1. 原图车 Tag 检测优先。
2. 成功后立即发送 UDP。
3. 鸟瞰兜底只在失败时执行。
4. 显示窗口完全低频化。

这一阶段是冲 60fps 的关键。

### 第四阶段：项目模块化

1. 拆出 `camera_worker.py`。
2. 拆出 `perf_stats.py`。
3. 拆出 `competition.py`。
4. 拆出显示和盲开逻辑。

这一阶段主要提升可维护性。

## 推荐比赛配置

```json
{
  "homography_update_mode": "once",
  "bird_eye_only": false,
  "display_enabled": true,
  "display_fps": 12,
  "show_original_view": true,
  "show_bird_eye_view": false,
  "show_track_map": true,
  "bird_eye_fallback_enabled": true,
  "fallback_bird_eye_width": 1600,
  "fallback_bird_eye_height": 1280,
  "display_bird_eye_width": 800,
  "display_bird_eye_height": 640
}
```

如果现场只追求最高发送频率，可以进一步关闭原图窗口：

```json
{
  "show_original_view": false,
  "show_bird_eye_view": false,
  "show_track_map": true
}
```

## 最终期望效果

理想比赛链路：

```text
摄像头 60fps 输入
采集线程持续刷新最新帧
处理主循环不积压旧帧
H 只初始化一次
原图检测成功直接发送 UDP
鸟瞰兜底按需执行
窗口 10~15fps 刷新
send_fps 接近 process_fps
process_fps 尽量接近 camera_fps
```

这样即使调试定位摄像头或官方定位模块达到 60Hz，代码也不会因为无意义显示和重复鸟瞰处理把发送频率拖下来。
