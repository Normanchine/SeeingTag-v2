# SeeingTag 本次定位优化说明

## 1. 本次目标

本次优化围绕两个现场问题展开：

1. 车载 ArUco Tag 在原始画面中偶尔识别不到；
2. 固定定位 Tag 为了更容易识别，可能需要横放、竖放或旋转摆放。

优化后的系统仍然使用单应矩阵（Homography）将摄像头像素坐标映射到 `X/Z` 场地坐标，并通过 UDP 将位置发送给 Unity。

## 2. 当前定位流程

```text
四个固定 Tag 同时识别
    ↓
取四个固定 Tag 的图像中心点
    ↓
与 tag_config.json 中的四个场地中心坐标建立对应关系
    ↓
计算并缓存 H（像素坐标 → 场地 X/Z 坐标）
    ↓
原始画面优先识别车载 Tag
    ├─ 识别成功：用 H 映射为车辆场地坐标
    └─ 识别失败：生成高分辨率鸟瞰图，在鸟瞰图中再次识别车载 Tag
                         ↓
                    直接换算为场地坐标
    ↓
视觉位置平滑 → 鸟瞰图显示
    ↓
UDP 输出平滑 → Unity
```

## 3. 固定 Tag：由角点方案改为中心点方案

### 3.1 为什么改

原实现使用每个固定 Tag 的四个角点计算 H。该方法隐含了一个条件：所有固定 Tag 的打印朝向必须和代码预期一致。

如果固定 Tag 在场地中旋转，即使 ArUco 可以正确读出 ID，图像角点与场地角点的对应关系也会错位，从而影响 H 的精度和稳定性。

### 3.2 现在的做法

现在每个固定 Tag 只贡献一对点：

```text
固定 Tag 的图像中心 (u, v)  →  固定 Tag 的场地中心 (X, Z)
```

四个固定 Tag 同时可见时，正好得到计算 H 所需的四对点。

### 3.3 现场摆放要求

- 固定 Tag 可以按识别效果横放、竖放或旋转；不需要额外填写旋转角度。
- 四个 Tag 必须平贴在同一个场地平面上，不能翘起或倾斜。
- `tag_config.json` 中每个固定 Tag 的 `x/z` 必须是该 Tag 的**中心**实际位置。
- 启动初始化和重新校准时，必须让四个固定 Tag 同时进入画面。

相较于使用全部角点，中心点方案的理论约束更少，但在本项目“启动时四码同时可见”的前提下更适合现场灵活摆码，且避免了朝向配置错误。

## 4. 车载 Tag 双通道识别

### 原图主路径

每帧首先在原始摄像头画面中检测车载 Tag。这是默认路径，速度最快，也不会受到透视变换插值的影响。

### 鸟瞰图兜底路径

若原图未检测到车载 Tag，程序会：

1. 使用缓存 H 生成 `1600 × 1280` 的高分辨率鸟瞰图；
2. 在该鸟瞰图中再次检测车载 Tag；
3. 成功后直接将鸟瞰图像素中心线性换算为场地 `X/Z` 坐标。

鸟瞰图仅在原图检测失败时生成，因此不会给正常帧增加额外开销。

### 短暂丢失保持

两条识别路径都失败时，程序会最多保持最近一次可信位置 `0.35` 秒。这样 Unity 中的车辆不会因为偶发丢帧立即消失或闪烁。

## 5. H 矩阵缓存保护

`interval` 和 `every_frame` 模式中，固定 Tag 可能在更新帧暂时离开画面。

现在只有成功计算出新的 H 时才会替换旧 H；新的计算失败时，继续使用上一次有效 H。这避免了“车 Tag 仍能识别，但因为固定码短暂丢失而无法定位”的问题。

## 6. 平滑与 UDP 状态

定位使用两层平滑：

| 层级 | 配置项 | 用途 |
|---|---|---|
| 视觉追踪层 | `filter_alpha` | 平滑鸟瞰图和 HUD 显示的车辆位置与朝向 |
| Unity 输出层 | `output_filter_alpha` | 平滑发送给 Unity 的 UDP 坐标与朝向 |

`alpha` 范围为 `0~1`：

- `1.0`：不平滑，直接使用最新测量值；
- 值越小：画面越平滑，但延迟越明显；
- 当前位置配置 `filter_alpha: 0.3`、`output_filter_alpha: 0.08`，后者会产生比较明显的 Unity 跟随延迟，现场可根据实际体验调大。

朝向角采用环绕平滑，跨越 `180°/-180°` 时不会出现绕一整圈的错误。

### UDP 新增字段

原有 `pos`、`euler`、`seq`、`timestamp` 保持不变，新增：

```json
"tracking_state": "raw"
```

可能取值：

| 值 | 含义 |
|---|---|
| `raw` | 原始画面直接识别到车载 Tag |
| `bird_eye_fallback` | 原图失败，由鸟瞰图兜底识别成功 |
| `hold` | 当前帧未识别，使用短暂保持的位置 |
| `blind` | 手动开启保护后，车载 Tag 丢失，正在按受限 CTRV 模型盲开 |
| `lost_hold` | 未开启盲开或盲开达到保护上限后，保持最后可信位置 |

Unity 不读取该字段时不会受影响；如需，可以利用它调整车辆显示或提示丢失状态。

## 7. 配置文件说明

本次新增或启用的配置项位于 `tag_config.json`：

```json
{
  "field_width_m": 5.0,
  "field_height_m": 4.0,
  "filter_alpha": 0.3,
  "output_filter_alpha": 0.08,
  "flip_x": false,
  "flip_z": false,
  "debug_logging": false
}
```

| 配置项 | 作用 |
|---|---|
| `field_width_m` / `field_height_m` | 场地尺寸；用于鸟瞰图边界、坐标翻转和显示 |
| `filter_alpha` | 视觉追踪平滑系数 |
| `output_filter_alpha` | UDP 输出平滑系数 |
| `flip_x` | 以场地 X 轴范围镜像 X 坐标，同时修正朝向和鸟瞰显示 |
| `flip_z` | 以场地 Z 轴范围镜像 Z 坐标，同时修正朝向和鸟瞰显示 |
| `debug_logging` | 是否输出高频 UDP、H 更新和兜底识别日志；比赛时建议关闭 |

`fov_degrees` 仍保留在配置中，但当前单应矩阵平面定位不使用相机视场角；该参数只在改用相机内参/PnP 方案时才会有意义。

## 8. 运行时状态提示

原始画面左下角的 `Track:` 表示当前帧的追踪来源：

| 显示 | 含义 |
|---|---|
| `RAW` | 原图识别成功 |
| `BIRD-EYE FALLBACK` | 鸟瞰图兜底识别成功 |
| `BLIND` | 已按 `1` 开启保护，车载 Tag 丢失后正在短时盲开 |
| `HOLD` | 使用最近一次可信位置 |
| `LOST HOLD` | 无识别且不再盲开，继续发送最后位置，避免 Unity 沿旧状态自走 |
| `LOST` | 已超过保持时间，当前无可用位置 |

左上角的 `BLIND PROTECT:` 表示盲开保护状态：

| 显示 | 含义 |
|---|---|
| `OFF` | 盲开保护关闭；识别不到时不会外推 |
| `ARMED` | 已按 `1` 开启一次保护；下一次车载 Tag 丢失时进入盲开 |
| `DRIVING` | 正在按受限 CTRV 模型盲开 |

## 9. 盲开保护参数

盲开保护只在运行时按 `1` 后生效，按 `0` 可关闭。车载 Tag 恢复识别后，保护会自动关闭并交回视觉定位。

当前盲开模型为受限 CTRV：根据近期速度和 yaw 角速度沿圆弧外推，同时对速度、转向和距离做保护，避免弯道出口继续大角度拐出线。

| 配置项 | 建议值 | 作用 |
|---|---:|---|
| `motion_history_seconds` | `3.0` | 保留多少秒历史样本供盲开估计 |
| `blind_drive_seconds` | `2.0` | 单次盲开最长时间 |
| `blind_max_distance_m` | `0.45` | 单次盲开最远距离；超过后自动转为 `LOST HOLD` |
| `blind_speed_window_seconds` | `0.5` | 用最近多少秒估计速度 |
| `blind_max_speed_mps` | `1.2` | 盲开速度上限 |
| `blind_yaw_rate_window_seconds` | `0.5` | 用最近多少秒估计 yaw 角速度 |
| `blind_yaw_rate_scale` | `0.35` | yaw 角速度缩放；拐太大就调小 |
| `blind_max_yaw_rate_dps` | `25.0` | yaw 角速度上限，单位 度/秒 |
| `blind_yaw_rate_decay_per_second` | `0.75` | 盲开时间越长，转向越趋向直线 |

若弯道出口仍然拐大，优先调小 `blind_yaw_rate_scale`，例如 `0.25`；若仍然过弯不足，再略微调大。不要把 `blind_drive_seconds` 设成无限大，建议用 `blind_max_distance_m` 覆盖实际盲区长度。

## 10. 现场建议

1. 优先保证车载 Tag 在原图中有足够像素尺寸；鸟瞰图兜底不能补回原始画面完全缺失的细节。
2. 固定 Tag 尽量贴平，中心坐标要按实际量取。
3. 初始化成功后，相机不可移动；若移动，请按 `R` 重新校准。
4. 若 Unity 中车辆明显滞后，先调大 `output_filter_alpha`，例如尝试 `0.2~0.4`。
5. 若画面位置抖动，尝试降低 `filter_alpha`；若响应迟缓，再调大它。

## 11. 特定旋转角度掉点修复

车载 Tag 沿圆轨迹运动时，曾在两个对称位置稳定丢失。实车检查确认，问题不是三维姿态解算，也不需要引入 PnP 或深度学习模型，而是检测结果后的尺寸筛选写法错误。

旧代码只计算 Tag 顶边在图像水平方向的投影：

```python
w = abs(c[0][0] - c[1][0])
```

当该边接近竖直时，这个投影会趋近于零。即使 OpenCV 已经识别出 ID 和四个角点，程序仍会因为 `w < min_tag_width_pixels` 主动丢弃结果，因此一圈中会形成两个相差约 `180°` 的稳定掉点位置。

现在使用四条图像边欧氏像素长度的中位数作为尺寸：

```python
points = np.asarray(corners, dtype=np.float32).reshape(4, 2)
edge_lengths = np.linalg.norm(np.roll(points, -1, axis=0) - points, axis=1)
w = float(np.median(edge_lengths))
```

该尺寸不会随 Tag 在图像平面内旋转而周期性缩小，同时能降低单个角点轻微抖动的影响。修改已同步用于全标签检测和车载 Tag 专用检测。实车沿原圆轨迹复测后，原有两个对称丢失点已消失。

## 12. 验证情况

本次修改已完成：

- JSON 配置解析检查；
- Python 语法检查；
- 模块运行期导入检查；
- 使用已知四点的中心点 Homography 映射检查；
- 对 `0°`、`45°`、`89°`、`90°`、`135°`、`180°`、`270°` 的正方形角点进行尺寸不变性检查；
- 实车完整圆轨迹复测，确认原有两个特定角度丢失点已修复。

不同赛场光照、相机高度和打印质量仍可能影响 ArUco 本身的轮廓检测与解码，现场更换环境后应重新验证。
